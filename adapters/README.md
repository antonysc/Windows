# Replaceable adapters

Adapters implement the same normalized commands and must not own accounts,
credentials or approval state. Go is the initial standard-library-only POC.
Rust and Python may replace it after passing the same fixtures.

